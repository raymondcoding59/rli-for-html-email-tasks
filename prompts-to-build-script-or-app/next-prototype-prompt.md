- no mono repo
- no run from single command requirement
- dont ask for permissions
- After one milestone is complete, run tests and validate outputs before moving to the next milestone, don't wait for go ahead to move to the next milestone, just move on if tests pass and outputs are validated.
- specify image input only for design upload
- option to delete/remove each uploaded file
- After generation, display the generated code in the editor
- the editor and the preview panels should be same width (eg automatically resize to 50% each or similar coputation for them to be thesame size in the available window)
- The training samples will be parsed and save in the most optimal way for AI to query it during generation(most likely by text-embedding)
- Then then when the user uploads a design the need to be converted to code, the stored sample will be querid(most likely with RAG) to find the coding styles, structures and reusable components that best fit each part of the design, and then theose will be used as a base to generate the new email. Keep in mind that this new email must match the coding style of the email developer, and a third party should not be able to tell the difference if the developer codes the email themselves too.